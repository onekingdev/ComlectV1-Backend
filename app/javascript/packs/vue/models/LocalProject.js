export default class LocalProject {
  constructor(business, business_id, cost, created_at, description, ends_on, hide_on_calendar, id, projects, reminders, starts_on, status, title, updated_at, visible_project) {
    this.business = business,
    this.business_id = business_id,
    this.cost = cost,
    this.created_at = created_at,
    this.description = description,
    this.ends_on = ends_on,
    this.hide_on_calendar = hide_on_calendar,
    this.id = id,
    this.projects = projects,
    this.reminders = reminders,
    this.starts_on = starts_on,
    this.status = status,
    this.title = title,
    this.updated_at = updated_at,
    this.visible_project = visible_project
  }
}


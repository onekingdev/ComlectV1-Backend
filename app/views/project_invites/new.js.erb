/*var $modal = $('#js-project-invite');
var modalContent = '<%= j render('new', invite: @invite) %>';
if ($modal.length > 0) {
  $modal
    .find('.modal-body').replaceWith($(modalContent).find('.modal-body')).end()
    .modal('show');
} else {
  $('body').append(modalContent);
  $('#js-project-invite').modal('show')
}

$(document).trigger('newContent', $('#js-project-invite'));
*/

_Modal.showPlain('<%= j render('new', invite: @invite) %>', { class: 'transparent' });
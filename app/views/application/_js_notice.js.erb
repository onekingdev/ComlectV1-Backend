options = {
  message: "<h2><%= local_assigns[:title] || 'Complect' %></h2><p><%= j message %></p>",
  backdrop: true,
  closeButton: false,
  className: 'notice <%= local_assigns[:class] %>'
};
bootbox.alert(options);

<% if @favorite.persisted? %>
$('[data-<%= @favorite.favorited.class.name %>-id=<%= @favorite.favorited_id %>]')
  .find('.card, .favorite').addClass('active').end();
<% else %>
$('[data-<%= @favorite.favorited.class.name %>-id=<%= @favorite.favorited_id %>]')
  .find('.card, .favorite').removeClass('active').end();
<% end %>

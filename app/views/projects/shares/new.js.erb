/*var $modal = $('#js-project-share');
var modalContent = '<%= j render('new', share: @share) %>';
if ($modal.length > 0) {
  $modal
    .find('form').replaceWith($(modalContent).find('form')).end()
    .modal('show');
} else {
  $('body').append(modalContent);
  $('#js-project-share').modal('show')
}

$(document).trigger('newContent', $('#js-project-share'));*/

_Modal.showPlain('<%= j render('new', share: @share) %>', { class: 'transparent' });
#js-project-share
  .modal-wrapper
    .modal-fake.gray.clearfix
      = simple_form_for share, url: project_shares_path(share.project.id), remote: true, wrapper: :inline_form, html: { class: 'modal-content' } do |f|
        h3.text-uppercase.text-center Share #{f.object.project.full_time? ? 'Job' : 'This Project' }
        .modal-body
          - if f.object.user.specialist
            .m-b-1.m-t-2= f.input :name, required: false, placeholder: f.object.user.business ? 'NAME' : 'TO', input_html: { class: 'input-lg' }
          = f.input :email, required: false, placeholder: "EMAIL", input_html: { class: 'input-lg m-t-2' }
          - if f.object.user.business
            .m-t-2
              = f.input :message,
                required: false,
                as: :text,
                input_html: { rows: 10 },
                hint: share.project.full_time? ? 'Note: A link to this job will be included at the bottom of this message upon sending.' : 'Note: A link to this project will be included at the bottom of this message upon sending.'
          = f.submit 'Share', class: 'btn btn-primary p-x-3 center-block m-b-2'

#sign-in
  .modal-wrapper
    .modal-fake.gray.clearfix
      button.close.close-absolute data-dismiss="modal" type="button"
        .fa.fa-close
      = simple_form_for resource, remote: true, as: resource_name, url: session_path(resource_name), html: { class: 'new_session' } do |f|

        - if flash[:warning]
          .text-danger.text-center.m-b-1= flash[:warning]

        = f.input :email, autofocus: true, label: false, input_html: { class: 'input-lg' }
        = f.input :password, label: false, input_html: { class: 'input-lg' }

        button.btn.btn-primary.center-block type="submit" Log In

        .text-center.forgot-password.m-t-1= link_to "Forgot Password", new_password_path(resource_name), remote: true, class: 'o-8 forgot-password'
        h4.text-center.text-uppercase.m-t-3.m-b-1
          | Don’t have an account yet? 
          = link_to "sign up here", new_user_registration_path, "data-remote" => true
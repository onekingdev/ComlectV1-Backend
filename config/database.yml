default: &default
  url: <%= ENV.fetch('DATABASE_URL') %>
  encoding: unicode
  pool: <%= ENV['DATABASE_POOL'].presence || 5 %>

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
